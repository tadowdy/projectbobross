import 'package:flutter/material.dart';

class CheckoutDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
          title: new Text('Checkout Details')
      ),
      body: Center(
        child: Text('Put the Checkout Details Here..'),
      ),
    );
  }
}
